import { errorHandler } from "./errorHandler"
import { notFound } from "./notFound"
import { authentication } from "./authentication"
import { setHasPartnerAuth } from "./setHasPartnerAuth"

export default {
  errorHandler,
  authentication,
  setHasPartnerAuth,
  notFound
}